-- prelude.air — loaded at bootstrap to expose intrinsics under AiR-canonical names.
-- Intrinsics live in Python (intrinsics.py) and are reachable via LOOKUP by their
-- Python names.  This file binds the names AiR code should actually use.

-- Logic
(define not not_fn)
(define and and_fn)
(define or  or_fn)
